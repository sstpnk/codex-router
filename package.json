{
  "name": "codex-router-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Thin local proxy: OpenAI Responses API (Codex) -> any OpenAI-compatible Chat Completions endpoint, with optional vision bridge",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "start": "node src/proxy/main.mjs"
  }
}
